# Test examples

This directory stores example folders and fixtures used by tests.

Add new example folders here when tests need representative file trees.

## Available examples

- `school/`: Sample dataset with `students.json`, `professors.json`, and `classes.json` resources.
