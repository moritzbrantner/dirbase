# Benchmark comparison snapshot

This file is intentionally not treated as a source of truth.

The benchmark now uses the synthetic multi-resource workload described in [`benchmarks/README.md`](./README.md) and writes fresh markdown reports to `benchmarks/results/benchmark-report-<timestamp>.md`.

To generate an up-to-date comparison locally, run:

```bash
scripts/benchmark_vs_json_server.sh
```
